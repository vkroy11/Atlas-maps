# Copy to `.env` (gitignored) and fill in values.

# MapTiler API key — used in Phase 2 for online tile fetches.
# Get one at https://cloud.maptiler.com/account/keys/
MAPTILER_KEY=

# Optional: override the local tile-server port on native (default: random)
# ATLAS_TILE_SERVER_PORT=
